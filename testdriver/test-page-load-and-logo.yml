version: 4.1.27
steps:
  - prompt: Focus the browser window.
    commands:
      - command: focus-application
        name: Google Chrome
  - prompt: Assert the webpage loads.
    commands:
      - command: assert
        expect: Login to Prompt LAIbrary is visible on the page
  - prompt: >-
      Assert the logo is displayed.  # Use the content of the file from the
      environment
    commands:
      - command: assert
        expect: the logo is visible on the page
